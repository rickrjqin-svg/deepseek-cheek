name: Build Android APK

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      release:
        description: "Publish a GitHub Release"
        required: true
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.11.1"
      - name: Create signing key
        run: >
          keytool -genkeypair -v -keystore app/release.keystore
          -storepass deepseek-balance -alias deepseek -keypass deepseek-balance
          -keyalg RSA -keysize 2048 -validity 10000
          -dname "CN=DeepSeek Balance, OU=Mobile, O=rickrjqin-svg, L=Shanghai, C=CN"
      - name: Build release APK
        run: gradle assembleRelease
      - name: Rename APK
        run: cp app/build/outputs/apk/release/app-release.apk DeepSeek-Balance-v1.1.1.apk
      - uses: actions/upload-artifact@v4
        with:
          name: DeepSeek-Balance-APK
          path: DeepSeek-Balance-v1.1.1.apk
      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v') || inputs.release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.1.1
          files: DeepSeek-Balance-v1.1.1.apk
          generate_release_notes: true
